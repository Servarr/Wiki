Other places to perform a replace:

Table "MetadataFiles", field "RelativePaths" - replace \\ with / as with other "\*Files" tables. Table "Config", "Key"="recyclebin" - "Value" field contains a path.
